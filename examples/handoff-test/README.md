# Example: Handoff Test

This example demonstrates the smallest useful multi-agent pattern:
- orchestrator creates the task brief
- builder writes the output and structured handoff
- reviewer validates clarity and completeness
- final summary is saved in the same task folder

Use this example when implementing the first real delegated workflow.
